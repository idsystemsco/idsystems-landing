<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IID Systems — Soluciones Inteligentes, Resultados Reales</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg:        #070C1A;
      --surface:   #0D1530;
      --surface-2: #132040;
      --text:      #EBF2FF;
      --muted:     #7A90BE;
      --accent:    #3D80EF;
      --accent-hi: #6DAAFF;
      --border:    #1C2E50;
      --nav-h:     64px;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
      font-size: 16px;
      line-height: 1.65;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }

    /* ── NAV ── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      height: var(--nav-h);
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 40px;
      background: rgba(7, 12, 26, 0.82);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-bottom: 1px solid rgba(28, 46, 80, 0.6);
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
    }

    .nav-wordmark {
      font-size: 20px;
      font-weight: 800;
      letter-spacing: -0.02em;
      line-height: 1;
    }

    .nav-wordmark .iid { color: var(--accent-hi); }
    .nav-wordmark .sys { color: var(--text); }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 32px;
    }

    .nav-links a {
      font-size: 14px;
      color: var(--muted);
      text-decoration: none;
      letter-spacing: .01em;
      transition: color .2s;
    }
    .nav-links a:hover { color: var(--text); }

    .nav-cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-size: 14px;
      font-weight: 600;
      color: var(--bg) !important;
      background: var(--accent);
      padding: 9px 20px;
      border-radius: 6px;
      text-decoration: none;
      transition: background .2s, transform .15s;
    }
    .nav-cta:hover { background: var(--accent-hi); transform: translateY(-1px); }

    /* ── HERO ── */
    #hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: calc(var(--nav-h) + 60px) 40px 80px;
      overflow: hidden;
    }

    #heroCanvas {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
    }

    .hero-content {
      position: relative;
      max-width: 820px;
    }

    .hero-label {
      font-family: 'SF Mono', 'Fira Code', 'Courier New', monospace;
      font-size: 12px;
      color: var(--accent);
      letter-spacing: .14em;
      text-transform: uppercase;
      margin-bottom: 28px;
    }

    .hero-content h1 {
      font-size: clamp(42px, 7vw, 82px);
      font-weight: 800;
      letter-spacing: -0.04em;
      line-height: 1.06;
      text-wrap: balance;
      margin-bottom: 28px;
      color: var(--text);
    }

    .hero-content h1 em {
      font-style: normal;
      color: var(--accent-hi);
    }

    .hero-sub {
      font-size: clamp(16px, 2vw, 19px);
      color: var(--muted);
      max-width: 52ch;
      line-height: 1.6;
      margin-bottom: 44px;
    }

    .hero-actions {
      display: flex;
      align-items: center;
      gap: 20px;
      flex-wrap: wrap;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      font-size: 15px;
      font-weight: 600;
      color: var(--bg);
      background: var(--accent);
      padding: 14px 28px;
      border-radius: 6px;
      text-decoration: none;
      transition: background .2s, transform .15s;
    }
    .btn-primary:hover { background: var(--accent-hi); transform: translateY(-2px); }

    .hero-proof {
      font-size: 13px;
      color: var(--muted);
      font-family: 'SF Mono', 'Fira Code', 'Courier New', monospace;
      letter-spacing: .04em;
    }

    .hero-proof span {
      color: var(--accent-hi);
      margin-right: 6px;
    }

    /* ── SECTIONS COMMON ── */
    section { padding: 96px 40px; }

    .section-eyebrow {
      font-family: 'SF Mono', 'Fira Code', 'Courier New', monospace;
      font-size: 11px;
      letter-spacing: .16em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 16px;
    }

    .section-title {
      font-size: clamp(28px, 4vw, 44px);
      font-weight: 800;
      letter-spacing: -0.03em;
      line-height: 1.1;
      text-wrap: balance;
      margin-bottom: 12px;
    }

    .section-sub {
      font-size: 17px;
      color: var(--muted);
      max-width: 52ch;
      margin-bottom: 56px;
    }

    /* ── SERVICES ── */
    #servicios { background: var(--surface); }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2px;
      max-width: 1200px;
      margin: 0 auto;
      border: 1px solid var(--border);
      border-radius: 8px;
      overflow: hidden;
    }

    .service-card {
      background: var(--bg);
      padding: 40px 36px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      border-right: 1px solid var(--border);
      transition: background .25s;
    }
    .service-card:last-child { border-right: none; }
    .service-card:hover { background: var(--surface-2); }

    .service-number {
      font-family: 'SF Mono', 'Fira Code', 'Courier New', monospace;
      font-size: 11px;
      color: var(--accent);
      letter-spacing: .12em;
    }

    .service-card h3 {
      font-size: 22px;
      font-weight: 700;
      letter-spacing: -0.02em;
      line-height: 1.2;
      color: var(--text);
    }

    .service-card p {
      font-size: 15px;
      color: var(--muted);
      line-height: 1.65;
      flex: 1;
    }

    .service-tag {
      display: inline-block;
      font-size: 11px;
      font-family: 'SF Mono', 'Fira Code', 'Courier New', monospace;
      letter-spacing: .08em;
      color: var(--accent-hi);
      background: rgba(61, 128, 239, 0.12);
      padding: 4px 10px;
      border-radius: 3px;
      width: fit-content;
    }

    /* ── PORTFOLIO ── */
    #portafolio { max-width: 1280px; margin: 0 auto; }

    .apps-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 1px;
      background: var(--border);
      border: 1px solid var(--border);
      border-radius: 8px;
      overflow: hidden;
    }

    .app-card {
      background: var(--bg);
      padding: 32px 28px;
      display: flex;
      flex-direction: column;
      gap: 12px;
      transition: background .2s;
      cursor: default;
    }
    .app-card:hover { background: var(--surface); }

    .app-category {
      font-family: 'SF Mono', 'Fira Code', 'Courier New', monospace;
      font-size: 10px;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: var(--muted);
    }

    .app-category.pos    { color: #5BAAFF; }
    .app-category.inv    { color: #4DD9A0; }
    .app-category.custom { color: #F0A050; }

    .app-name {
      font-size: 20px;
      font-weight: 700;
      letter-spacing: -0.02em;
      color: var(--text);
      line-height: 1.2;
    }

    .app-desc {
      font-size: 14px;
      color: var(--muted);
      line-height: 1.6;
    }

    .app-status {
      margin-top: auto;
      padding-top: 16px;
      display: flex;
      align-items: center;
      gap: 7px;
      font-size: 12px;
      color: var(--muted);
    }

    .status-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: #4DD9A0;
      flex-shrink: 0;
    }

    /* ── CTA BAND ── */
    #contacto {
      background: var(--surface);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      text-align: center;
    }

    .cta-inner {
      max-width: 640px;
      margin: 0 auto;
    }

    .cta-inner h2 {
      font-size: clamp(28px, 4vw, 44px);
      font-weight: 800;
      letter-spacing: -0.03em;
      line-height: 1.1;
      text-wrap: balance;
      margin-bottom: 16px;
    }

    .cta-inner p {
      font-size: 17px;
      color: var(--muted);
      margin-bottom: 36px;
    }

    /* ── FOOTER ── */
    footer {
      padding: 32px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 12px;
      border-top: 1px solid var(--border);
    }

    .footer-brand {
      font-size: 14px;
      font-weight: 700;
      letter-spacing: -0.01em;
    }
    .footer-brand .iid { color: var(--accent-hi); }

    footer p {
      font-size: 13px;
      color: var(--muted);
    }

    footer a {
      color: var(--accent-hi);
      text-decoration: none;
    }
    footer a:hover { text-decoration: underline; }

    /* ── RESPONSIVE ── */
    @media (max-width: 680px) {
      nav { padding: 0 20px; }
      .nav-links a:not(.nav-cta) { display: none; }
      #hero { padding: calc(var(--nav-h) + 40px) 20px 60px; }
      section { padding: 72px 20px; }
      .service-card { border-right: none; border-bottom: 1px solid var(--border); }
      .service-card:last-child { border-bottom: none; }
      footer { flex-direction: column; align-items: flex-start; }
    }

    /* ── FOCUS ── */
    a:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 3px;
      border-radius: 3px;
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo" aria-label="IID Systems">
    <svg width="36" height="36" viewBox="0 0 48 48" fill="none" aria-hidden="true">
      <defs>
        <linearGradient id="rg" x1="0" y1="0" x2="48" y2="48" gradientUnits="userSpaceOnUse">
          <stop offset="0%" stop-color="#1E6FD4"/>
          <stop offset="100%" stop-color="#6DAAFF"/>
        </linearGradient>
      </defs>
      <circle cx="22" cy="25" r="19" stroke="url(#rg)" stroke-width="2.2" fill="none"/>
      <text x="22" y="31" text-anchor="middle" font-family="-apple-system,system-ui,sans-serif"
            font-weight="900" font-style="italic" font-size="16" fill="url(#rg)">IID</text>
      <circle cx="36" cy="10" r="2.2" fill="#6DAAFF" opacity=".9"/>
      <circle cx="42" cy="10" r="2.2" fill="#6DAAFF" opacity=".6"/>
      <circle cx="36" cy="16" r="2.2" fill="#6DAAFF" opacity=".5"/>
      <circle cx="42" cy="16" r="2.2" fill="#6DAAFF" opacity=".3"/>
    </svg>
    <span class="nav-wordmark">
      <span class="iid">IID</span><span class="sys">Systems</span>
    </span>
  </a>
  <div class="nav-links">
    <a href="#servicios">Servicios</a>
    <a href="#portafolio">Portafolio</a>
    <a href="https://wa.me/50248810078" class="nav-cta" target="_blank" rel="noopener">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
      </svg>
      WhatsApp
    </a>
  </div>
</nav>

<!-- HERO -->
<section id="hero">
  <canvas id="heroCanvas" aria-hidden="true"></canvas>
  <div class="hero-content">
    <p class="hero-label">IID Systems · Guatemala, Centroamérica</p>
    <h1>Software que trabaja cuando tu negocio <em>no para.</em></h1>
    <p class="hero-sub">Desarrollamos aplicaciones para restaurantes, cafeterías y negocios. Desde el punto de venta hasta el control de inventario — todo bajo tu marca.</p>
    <div class="hero-actions">
      <a href="https://wa.me/50248810078" class="btn-primary" target="_blank" rel="noopener">
        Hablemos por WhatsApp
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <p class="hero-proof"><span>●</span> 6 aplicaciones en producción</p>
    </div>
  </div>
</section>

<!-- SERVICIOS -->
<section id="servicios">
  <div style="max-width:1200px; margin:0 auto;">
    <p class="section-eyebrow">Servicios</p>
    <h2 class="section-title">Lo que construimos</h2>
    <p class="section-sub">Soluciones reales para negocios reales — sin configuraciones complicadas ni contratos largos.</p>
    <div class="services-grid">

      <div class="service-card">
        <span class="service-number">01</span>
        <h3>POS para Restaurantes</h3>
        <p>Sistema de punto de venta para iPad. Gestiona mesas, órdenes, cobros y reportes del día. Funciona sin internet y sincroniza cuando vuelve la conexión.</p>
        <span class="service-tag">White-label disponible</span>
      </div>

      <div class="service-card">
        <span class="service-number">02</span>
        <h3>Control de Inventario</h3>
        <p>Lleva el registro de entradas, salidas y existencias de tu cafetería o negocio desde el teléfono. Simple, rápido y siempre actualizado.</p>
        <span class="service-tag">iOS · Android</span>
      </div>

      <div class="service-card">
        <span class="service-number">03</span>
        <h3>Desarrollo a Medida</h3>
        <p>Si tienes un proceso que automatizar o una idea que llevar al teléfono, lo construimos. Desde herramientas internas hasta apps de cara al cliente.</p>
        <span class="service-tag">Proyecto personalizado</span>
      </div>

    </div>
  </div>
</section>

<!-- PORTAFOLIO -->
<section id="portafolio" style="padding-top:96px; padding-bottom:96px;">
  <div style="max-width:1200px; margin:0 auto;">
    <p class="section-eyebrow">Portafolio</p>
    <h2 class="section-title">Aplicaciones en producción</h2>
    <p class="section-sub">Cada app en uso real, con clientes reales, en Guatemala.</p>

    <div class="apps-grid">

      <div class="app-card">
        <span class="app-category pos">POS · Restaurante</span>
        <p class="app-name">Makarios POS</p>
        <p class="app-desc">Sistema de punto de venta para la cafetería Makarios Parce. Mesas, split bills, cobros en efectivo y tarjeta, impresión de comandas.</p>
        <div class="app-status"><span class="status-dot"></span> En producción · iOS</div>
      </div>

      <div class="app-card">
        <span class="app-category pos">POS · Restaurante</span>
        <p class="app-name">Yexi POS</p>
        <p class="app-desc">Plataforma POS white-label para Yexi. Mismas funcionalidades del sistema base, adaptadas con la identidad del cliente.</p>
        <div class="app-status"><span class="status-dot"></span> En producción · iOS</div>
      </div>

      <div class="app-card">
        <span class="app-category pos">POS · Restaurante</span>
        <p class="app-name">Yeguada POS</p>
        <p class="app-desc">Versión white-label para La Yeguada. Gestión completa de mesas, órdenes y reportes diarios bajo la marca del restaurante.</p>
        <div class="app-status"><span class="status-dot"></span> En producción · iOS</div>
      </div>

      <div class="app-card">
        <span class="app-category pos">POS · Restaurante</span>
        <p class="app-name">BH POS</p>
        <p class="app-desc">Sistema de punto de venta para Buen Hábito. Flujo de cobro, descuentos, historial y panel de administración integrado.</p>
        <div class="app-status"><span class="status-dot"></span> En producción · iOS</div>
      </div>

      <div class="app-card">
        <span class="app-category inv">Inventario</span>
        <p class="app-name">Makarios Inventario</p>
        <p class="app-desc">Control de inventario de productos para la cafetería Makarios Parce. Entradas, salidas y existencias en tiempo real desde el celular.</p>
        <div class="app-status"><span class="status-dot"></span> En producción · iOS</div>
      </div>

      <div class="app-card">
        <span class="app-category custom">A Medida</span>
        <p class="app-name">School Tasks</p>
        <p class="app-desc">Aplicación de seguimiento de tareas escolares. Muestra cómo llevamos ideas personales a apps funcionales en tiempo récord.</p>
        <div class="app-status"><span class="status-dot"></span> En uso · iOS</div>
      </div>

    </div>
  </div>
</section>

<!-- CTA -->
<section id="contacto">
  <div class="cta-inner">
    <p class="section-eyebrow" style="text-align:center;">Contacto</p>
    <h2>¿Listo para digitalizar tu negocio?</h2>
    <p>Cuéntanos qué necesitas. Respondemos rápido y sin rodeos.</p>
    <a href="https://wa.me/50248810078" class="btn-primary" target="_blank" rel="noopener"
       style="margin: 0 auto; display: inline-flex;">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
      </svg>
      Escribir por WhatsApp
    </a>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <span class="footer-brand"><span class="iid">IID</span>Systems</span>
  <p>Soluciones Inteligentes, Resultados Reales.</p>
  <p>© 2026 · <a href="https://wa.me/50248810078" target="_blank" rel="noopener">+502 4881-0078</a></p>
</footer>

<script>
  (function () {
    const canvas = document.getElementById('heroCanvas');
    if (!canvas) return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    const ctx = canvas.getContext('2d');
    let w, h, particles;

    function resize() {
      w = canvas.width  = canvas.offsetWidth;
      h = canvas.height = canvas.offsetHeight;
    }

    function makeParticle() {
      return {
        x: Math.random() * w,
        y: Math.random() * h,
        r: Math.random() * 1.4 + 0.4,
        vx: (Math.random() - 0.5) * 0.25,
        vy: -(Math.random() * 0.28 + 0.08),
        op: Math.random() * 0.22 + 0.04,
      };
    }

    function init() {
      resize();
      particles = Array.from({ length: 55 }, makeParticle);
    }

    function tick() {
      ctx.clearRect(0, 0, w, h);
      for (const p of particles) {
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(109,170,255,${p.op})`;
        ctx.fill();
        p.x += p.vx;
        p.y += p.vy;
        if (p.y < -4) { p.y = h + 4; p.x = Math.random() * w; }
        if (p.x < -4) p.x = w + 4;
        if (p.x > w + 4) p.x = -4;
      }
      requestAnimationFrame(tick);
    }

    init();
    tick();
    window.addEventListener('resize', init);
  })();
</script>

</body>
</html>
